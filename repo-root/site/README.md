# site
静的サイト実装ディレクトリです。

## Admin Login
- Email: `admin@example.com`
- Password: `password`

## API Base URL
管理画面が呼び出す API の URL は `assets/js/ops-config.js` の `apiBase` で設定します。
例:
```js
apiBase: "https://your-service.onrender.com"
```
