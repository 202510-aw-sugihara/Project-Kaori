spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:shizuka-test;DB_CLOSE_DELAY=-1;MODE=MySQL;DATABASE_TO_UPPER=false
    driver-class-name: org.h2.Driver
    username: sa
    password:

logging:
  level:
    root: WARN
    com.example.shizuka: DEBUG
